TODO:
- add search bar
- call backend
- graphql  to return more complex results
- put koi fish logo in search bar 


searchbar:
- dynamic search, so results come as u search?
-  suggested searches, how does google do it, onchange search text,
- form
- input 
- onchange handler 

results:
- return white material ui cards
- animation on return
- move search bar and show results on whole page?

now:
- work on query to backend, GRAPHQL?
- show results, link + content?
- add some animation to search bar and results or something?
- animation to move search bar + suggestions, then go back and work on backend todo

graphql:
- can be used for images and stuff later, other types of searches
- for now, only one root query that will call elastic search match
